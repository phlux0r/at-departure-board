name: CI

on:
  push:
    branches: [main, master]
  pull_request:

jobs:
  renderer:
    name: Renderer and tools (pytest)
    # Windows, not Linux: tools/board/render.py loads Arial and Consolas from
    # %WINDIR%/Fonts and silently falls back to Pillow's default font when they
    # are missing, so the byte-exact goldens only reproduce on Windows.
    runs-on: windows-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          # Pinned: the golden renders are compared byte-exact, so the Pillow
          # in requirements-dev.txt has to be the one it was built against.
          python-version: '3.11'
          cache: pip
          cache-dependency-path: requirements-dev.txt
      - run: python -m pip install -r requirements-dev.txt
      - run: python -m pytest -v

  firmware-tests:
    name: Firmware logic (pio test -e native)
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: pip
          cache-dependency-path: requirements-dev.txt
      - uses: actions/cache@v4
        with:
          path: ~/.platformio
          key: pio-native-${{ hashFiles('platformio.ini') }}
      - run: python -m pip install platformio
      - run: pio test -e native

  firmware-build:
    name: Firmware builds (esp32, esp32s3)
    runs-on: ubuntu-latest
    # Longer than the other jobs: a cold cache downloads two toolchains, one
    # per target architecture.
    timeout-minutes: 25
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: pip
          cache-dependency-path: requirements-dev.txt
      - uses: actions/cache@v4
        with:
          path: ~/.platformio
          key: pio-esp32-${{ hashFiles('platformio.ini') }}
      - run: python -m pip install platformio
      # Every build includes src/secrets.h, which is gitignored. The unedited
      # template compiles, which is all a build check needs.
      - run: cp src/secrets.example.h src/secrets.h
      - run: pio run -e esp32
      - run: pio run -e esp32_demo
      - run: pio run -e esp32s3
      - run: pio run -e esp32s3_demo
